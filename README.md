# SocialVideoSentimentAnalysis_System

Workflow (order in which we update files)
1. config.yaml
2. entity
3. config/configuration.py
4. componenets
5. pipeline
6. main.py // just to test the pipeline
7. app.py

# How to run

### Step 01 - create a conda environment after opening the repository

```bash```

conda create -name SocialVideoSentimentAnalysisSystem python=3.11 -y

```bash```
conda activate SocialVideoSentimentAnalysisSystem

### Step 02- install the requirements

```ignore if you have installed anaconda```

pip install -r requirements.txt

### Run app.py file

``` app.py```